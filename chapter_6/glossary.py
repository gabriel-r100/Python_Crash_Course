glossary = {
    'print()': 'outputs to the terminal',
    '.title()': 'uses title case on a string',
    'for loop': 'iterates through an array',
    'if statements': 'validates a condition',
    'elif statements': 'tests an alternate condition to an if statemen',
    'else': 'when other conditions are not met, this action is performed'
}

print(f"The definition of print() is:\n\t{glossary['print()']}")
print(f"The definition of .title() is:\n\t{glossary['.title()']}")
print(f"The definition of a for loop is:\n\t{glossary['for loop']}")
print(f"The definition of if statements are:\n\t{glossary['if statements']}")
print(f"The definition of elif statements are:\n\t{glossary['elif statements']}")
print(f"The definition of .an else statement is:\n\t{glossary['else']}")
